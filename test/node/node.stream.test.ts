const fs = require('fs');

describe('Node Stream test', () => {

  it('Test FS', async () => {
    // const a = ['Hello', 'hi'];
    // const b = ['World', 'bye'];

    // const filteredArray = Array.from(a[0]).filter(value => Array.from(b[0]).includes(value));
    // console.log(filteredArray);
    const contents = 'afawfe';
    const a = JSON.stringify(contents);
    console.log('a = ', a);
  });
});

